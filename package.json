{
  "name": "ai-widget-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AI Customer Support Widget - Multi-tenant SaaS Platform",
  "workspaces": [
    "backend",
    "frontend",
    "widget"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=widget",
    "test": "npm run test --workspace=backend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
